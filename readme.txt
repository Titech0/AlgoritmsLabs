Написати алгоритми за допомогою циклу та за допомогою рекурсії:

1. Підрахунок факторіалу N чисел.
2. Підрахунок N-го числа Фібоначчі.

вирішити за допомогою рекурсії:
3. Обчисліть суму цифр числа N, що надходить на вхід алгоритму. При вирішенні цього завдання не можна використовувати строки, списки, масиви (та й цикли, зрозуміло).
4. Напишіть рекурсивну функцію для обчислення суми заданих позитивних цілих чисел a та b без прямого використання оператора +.


Перші дві задачі оформити у вигляді окремих класів з методами recursionExecute та cycleExecute, які викликаються з Main.
Результат роботи кожного методу з класу зберігати в змінну та в кожному завданні виконувати перевірку
assert a == b: "different values";
Після чого вивести N та один із результатів.

! УВАГА, для роботи assert під час запуску програми треба вказувати ключ -ea


задачі 3 та 4 виконати також в окремих класах з методом execute(...);

Ці методи викликаються з main і потім друкуються:
- Вихідні параметри
- результат

